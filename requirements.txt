# Refacer — direct dependencies only.
# pip resolves transitive deps (torch, facexlib, basicsr, etc.) automatically
# and picks wheels that match your platform and Python version.
#
# macOS: works on both Apple Silicon (arm64) and Intel (x86_64).
# Requires Python 3.10+.

insightface>=0.7.3
onnxruntime>=1.16.0
gfpgan>=1.3.8
gradio>=4.0
opencv-python-headless>=4.5
numpy>=1.24
