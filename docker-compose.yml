services:
  refacer:
    build: .
    image: refacer
    ports:
      - "7860:7860"
    environment:
      - REFACER_DOCKER=1
    volumes:
      - ./models:/refacer/models   # must contain inswapper_128.onnx
      - ./input:/refacer/input
      - ./output:/refacer/output
